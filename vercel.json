{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "functions": {
    "api/cpanel.js": {
      "maxDuration": 60
    },
    "api/live-preview.js": {
      "maxDuration": 30
    }
  },
  "rewrites": [
    { "source": "/:path*", "destination": "/index.html" }
  ]
}
