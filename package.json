{
  "name": "reactcms-platform",
  "private": true,
  "version": "1.0.0",
  "packageManager": "npm@10.8.2",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build && node scripts/copy-build.js",
    "dev": "turbo run dev",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "tsup": "^8.2.4",
    "turbo": "^2.0.14",
    "typescript": "^5.5.4",
    "vitest": "^2.0.5"
  },
  "dependencies": {
    "@anshif.rainhopes/reactcms-runtime": "^1.0.4",
    "@anshif.rainhopes/reactcms-sdk": "^1.0.5"
  }
}
